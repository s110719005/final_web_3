$(document).ready(function () {

    // Load JSON file
    let openingAnimWindow = document.querySelector('#openingLottie');
    let openingAnimData = {
      container: openingAnimWindow,
      animType: 'svg',
      loop: true,
      prerender: true,
      autoplay: true,
      // path: 'json/download-icon.json'
      path: 'https://assets9.lottiefiles.com/datafiles/S8iOn0PKnic5RCO/data.json'
    };
    // set bodymovin
    let openingAnim = bodymovin.loadAnimation(openingAnimData);
  
  });

  $(document).ready(function () {

    // Load JSON file
    let openingAnimWindow = document.querySelector('#openingLottie2');
    let openingAnimData = {
      container: openingAnimWindow,
      animType: 'svg',
      loop: true,
      prerender: true,
      autoplay: true,
      // path: 'json/download-icon.json'
      path: 'https://assets9.lottiefiles.com/datafiles/S8iOn0PKnic5RCO/data.json'
    };
    // set bodymovin
    let openingAnim = bodymovin.loadAnimation(openingAnimData);
  
  });

  $(document).ready(function () {

    // Load JSON file
    let openingAnimWindow = document.querySelector('#openingLottie3');
    let openingAnimData = {
      container: openingAnimWindow,
      animType: 'svg',
      loop: true,
      prerender: true,
      autoplay: true,
      // path: 'json/download-icon.json'
      path: 'https://assets9.lottiefiles.com/datafiles/S8iOn0PKnic5RCO/data.json'
    };
    // set bodymovin
    let openingAnim = bodymovin.loadAnimation(openingAnimData);
  
  });

  $(document).ready(function () {

    // Load JSON file
    let openingAnimWindow = document.querySelector('#openingLottie4');
    let openingAnimData = {
      container: openingAnimWindow,
      animType: 'svg',
      loop: true,
      prerender: true,
      autoplay: true,
      // path: 'json/download-icon.json'
      path: 'https://assets9.lottiefiles.com/datafiles/S8iOn0PKnic5RCO/data.json'
    };
    // set bodymovin
    let openingAnim = bodymovin.loadAnimation(openingAnimData);
  
  });

  $(document).ready(function () {

    // Load JSON file
    let openingAnimWindow = document.querySelector('#openingLottie5');
    let openingAnimData = {
      container: openingAnimWindow,
      animType: 'svg',
      loop: true,
      prerender: true,
      autoplay: true,
      // path: 'json/download-icon.json'
      path: 'https://assets2.lottiefiles.com/datafiles/AembVTvov5PkTSJ/data.json'
    };
    // set bodymovin
    let openingAnim = bodymovin.loadAnimation(openingAnimData);
  
  });